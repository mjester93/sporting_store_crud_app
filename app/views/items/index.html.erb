<h1>Items</h1>

<ul>
    <% @items.each do |item| %>
        <li><%= link_to item.name, item_path(item.id) %></li>
    <% end %>
</ul>
<br>
<div>
    <%= link_to "Add New Item", new_item_path %>
</div>